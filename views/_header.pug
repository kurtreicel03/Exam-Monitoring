nav.navbar.navbar-expand-lg.navbar-dark.bg-success
    .container-fluid
        a.navbar-brand(href=`${user ? '/home': '/'}`) EXAM MONITORING
        button.navbar-toggler(type='button' data-bs-toggle='collapse' data-bs-target='#navbarSupportedContent' aria-controls='navbarSupportedContent' aria-expanded='false' aria-label='Toggle navigation')
            span.navbar-toggler-icon
        #navbarSupportedContent.collapse.navbar-collapse
            ul.navbar-nav.me-auto.mb-2.mb-lg-0
                if user
                   if role === 'admin'
                    li.nav-item
                        a.nav-link(href='/home' class=`${path === 'home' ? 'active' : ''}`) My Reports
                    li.nav-item
                        a.nav-link(href='/manage-reports' class=`${path === 'manage' ? 'active' : ''}`) Manage Reports
                    li.nav-item
                        a.nav-link(href='/users' class=`${path === 'users' ? 'active' : ''}`) Manage Users  
                   else
                    li.nav-item
                        a.nav-link(href='/home' class=`${path === 'home' ? 'active' : ''}`) My Reports
                    li.nav-item
                        a.nav-link(href='/create' class=`${path === 'create' ? 'active' : ''}`) Create Report
                    li.nav-item
                        a.nav-link(href=`/account/${user._id ? user._id.toString() : '123'}` class=`${path === 'users' ? 'active' : ''}`) My Profile    

                